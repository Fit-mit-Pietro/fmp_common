part of questionnaire_service;

// extension ApiFilledQuestionExtension on api.FilledQuestion {
//   static api.FilledQuestion fromServiceQuestion(FilledQuestion question) =>
//       api.FilledQuestion(
//           id: question.id,
//           userProperty: question.userProperty.toAPIProperty(),
//           text: question.text);

//   FilledQuestion toServiceQuestion() => FilledQuestion(
//       id: id, userProperty: userProperty.fromServiceProperty(), text: text);
// }

// extension ServiceFilledQuestionExtension on FilledQuestion {
//   static FilledQuestion fromApiQuestion(api.FilledQuestion question) =>
//       FilledQuestion(
//           id: question.id,
//           userProperty: question.userProperty.fromServiceProperty(),
//           text: question.text);

//   api.FilledQuestion toApiQuestion() => api.FilledQuestion(
//       id: id, userProperty: userProperty.toAPIProperty(), text: text);
// }
